/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Raw conference photos dumped in the project root (huge). The optimized hero
# lives in /public and IS committed; these originals are not.
/*.jpg
/*.jpeg
